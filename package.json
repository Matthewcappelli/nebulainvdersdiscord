{
  "name": "sakura-invaders-discord",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/game.js --bundle --format=esm --outfile=dist/game.js",
    "start": "npm run build && node server.mjs"
  },
  "dependencies": {
    "@discord/embedded-app-sdk": "^2.5.0",
    "discord.js": "^14.26.4",
    "esbuild": "^0.28.0",
    "pg": "^8.21.0"
  }
}
